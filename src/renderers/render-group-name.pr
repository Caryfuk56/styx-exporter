{*
* Render a comment for the group name.
* @param {TokenGroup} context - the token group of the current group.
*}
{[ const comment = groupNameComment(context) /]}
{[ if (comment) ]}

{{ comment }}
{[/]}