{
  "name": "supernova-exporter-stylex-css",
  "version": "2.0.0",
  "description": "Javascript computation for stylex & css all tehemes exporters.",
  "main": "index.js",
  "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
      "build": "NODE_OPTIONS=--openssl-legacy-provider webpack --mode=production",
      "watch": "NODE_OPTIONS=--openssl-legacy-provider webpack --mode=production --watch",
      "watch:dev": "NODE_OPTIONS=--openssl-legacy-provider webpack --mode=development --watch"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
      "ts-loader": "^7.0.5",
      "typescript": "^3.9.9",
      "webpack": "^4.46.0",
      "webpack-cli": "^3.3.12"
  }
}